// ---------create table---------
// users
// CREATE TABLE users (id serial, f_name text, l_name text, email text, password text, primary key (id));
// beers
// CREATE TABLE posts (id serial, user_id int references users(id), name text, brewery text, style text, untapped boolean);


// ---------create table---------